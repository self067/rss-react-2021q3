import styled from 'styled-components';

const ValidateStatus = styled.span`
  color: red;
  font-weight: bold;
  font-size: 10px;
  /* margin-top: -5px; */
  text-align: center;
`;
export default ValidateStatus;
